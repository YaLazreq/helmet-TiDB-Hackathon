# from mcp.server.fastmcp import FastMCP
# from tools. import distance_matrix

# mcp = FastMCP("Google Maps MCP Server")


# @mcp.tool()
# def distance_matrix(origins: str, destinations: str, mode: str = "driving") -> str:
#     """Get distance matrix between origins and destinations"""
#     return
#     # return t.distance_matrix_tool.run(
#     #     origins=origins, destinations=destinations, mode=mode
#     # )


# @mcp.tool()
# def places_nearby(
#     location: str, radius: int = 1000, place_type: str = "restaurant"
# ) -> str:
#     """Find nearby places"""
#     # return t.places_nearby_tool.func(
#     #     location=location, radius=radius, place_type=place_type
#     # )


# @mcp.tool()
# def geocode(address: str) -> str:
#     """Convert address to coordinates"""
#     # return t.geocode_tool.func(address=address)


# @mcp.tool()
# def reverse_geocode(lat: float, lng: float) -> str:
#     """Convert coordinates to address"""
#     # return t.reverse_geocode_tool.func(lat=lat, lng=lng)
